# Basic Page

This fixture is only a shape prototype.

It demonstrates the intended Rossi-first contract:

- source comes from `source.tsx`
- Rossi is expected to compile and execute it
- assertions focus on stable outputs instead of custom test scripts

Once a real runner exists, this folder will likely gain a small declarative
fixture manifest to describe execution mode and optional thread-specific props.
